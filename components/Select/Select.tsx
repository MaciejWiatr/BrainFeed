import React from "react";
import { StyleSheet, Text, View } from "react-native";

const Select = ({ options, opened }) => {
	return (
		<View>
			<Text></Text>
		</View>
	);
};

export default Select;

const styles = StyleSheet.create({});
